// Frontend runtime config (same pattern as the other Nerd sites' env.js).
// Edit API_URL to point at the backend:
//   - Local dev:  http://127.0.0.1:8000
//   - Production: https://api.hallucinationnerd.org
window.env = {
    // API_URL: "http://127.0.0.1:8000",
    API_URL: "https://api.hallucinationnerd.org"
};
